% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arima.R
\name{arima.tendency.chart}
\alias{arima.tendency.chart}
\title{绘制时间序列趋势图（包含均值 ± 2 标准差的置信区间）}
\usage{
arima.tendency.chart(data, observe, title = "", breaks_by = "year")
}
\arguments{
\item{data}{T: data.frame[required.col.names <- c(date: Date, observe: num)]
时间序列数据集，`date` 列必须是 `Date` 类型，`observe` 列必须是数值类型}

\item{observe}{T: char 指定需要观察的列名（必须是 `data` 中的数值列）}

\item{title}{F: char （默认 `""`）图表标题}

\item{breaks_by}{F: char （默认 `"year"`）x 轴刻度的间隔，可以是 `"month"`, `"quarter"`, `"year"` 等}
}
\value{
ggplot 对象，绘制时间序列趋势图
}
\description{
本函数用于绘制时间序列数据的趋势图，包含：
- 原始数据的折线图
- 均值 ± 2 标准差的灰色置信区间
- 关键时间刻度（默认按年份）
}
\examples{
# 示例数据
file.time <- data.frame(
  date = seq(as.Date("2020-01-01"), by = "month", length.out = 36),
  incidence.rate = rnorm(36, mean = 50, sd = 10)
)

# 绘制趋势图
arima.tendency.chart(file.time, "incidence.rate", title = "Incidence Rate Trend")
}
